# Benchmark corpus

This directory contains only the schema and protocol. No sample or score is fabricated.

Place approved test images and ground truth outside source control when they contain sensitive data.
The device benchmark output must identify samples by stable ID and record the app commit and model
hashes. See `docs/BENCHMARK_PROTOCOL.md`.
